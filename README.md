# intro section with dropdown navigation using vite and sass
This solotion uses vite, sass, normalize.css, and vanilla js
## commands
1. run the development server `npm run dev`
2. build `npm run build` 

